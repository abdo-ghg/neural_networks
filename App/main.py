from models import SLP, ADALine

'''
==> task 3

input 
3 files depend on the user choose which 2 classes he want to test on

Task description
Train and evaluate the SLP and ADALine models on the preprocessed penguin dataset.
models will be trained on the training set and evaluated on the test set.
make a train test split for the dataset and then train the models on the training set and evaluate them on the test set.
don't forget to shuffle the dataset before splitting it into training and test sets.

output
the accuracy of the model on the test set and the confusion matrix
and weights
the line between the 2 classes in the 2D space
'''